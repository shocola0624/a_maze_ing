[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mazegen"
version = "2026.3.6"
requires-python = ">= 3.10"
description = "This is a maze generator"
readme = "README.md"
dependencies = []
